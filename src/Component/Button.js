import * as React from 'react';
import Button from '@mui/material/Button';

export default function NyButton() {
  return (
    <div>
      <Button color= "secondary" variant="contained">Hello World</Button>
    </div>
  );
}